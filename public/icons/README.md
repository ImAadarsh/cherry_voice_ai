# App icons

Add `icon-192.png` (192×192) and `icon-512.png` (512×512) here for PWA install
prompts. They are referenced by `/public/manifest.webmanifest`. Any square PNG
with the Cherry brand color (`#e11d48`) works; use a maskable-safe padding.
