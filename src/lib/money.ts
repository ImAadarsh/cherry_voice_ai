/**
 * @deprecated Import from `@/lib/currency` instead.
 * Re-exports kept for backward compatibility.
 */
export {
  minorUnitFactor,
  toMinor,
  toMajor,
  formatMoney,
  formatMajor,
  DEFAULT_CURRENCY,
} from "./currency";
